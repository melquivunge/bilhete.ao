server {
    listen 80;
    server_name _;

    root /var/www/html/public;
    index index.php;

    charset utf-8;

    client_max_body_size 20M;

    # Não revelar a versão do Nginx nas respostas nem nas páginas de erro.
    server_tokens off;

    # Cabeçalhos baratos agora, caros de acrescentar quando já houver páginas de
    # checkout e conteúdo de terceiros (Marcos 3 e 4).
    # Strict-Transport-Security fica para quando existir TLS real, no Marco 7.
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "DENY" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Content-Security-Policy "default-src 'self'; frame-ancestors 'none'" always;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location = /favicon.ico { access_log off; log_not_found off; }
    location = /robots.txt  { access_log off; log_not_found off; }

    error_page 404 /index.php;

    # Só o front controller é executado. Qualquer outro .php sob a document root
    # devolve 404 em vez de ser entregue ao PHP-FPM: se um upload alguma vez
    # aterrar em public/, não há caminho de execução.
    location ~ ^/index\.php$ {
        fastcgi_pass app:9000;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param DOCUMENT_ROOT $document_root;
        fastcgi_hide_header X-Powered-By;
    }

    location ~ \.php$ {
        return 404;
    }

    # Ficheiros ocultos nunca são servidos. Sem esta regra, um .env colocado por
    # engano dentro de public/ seria descarregável.
    location ~ /\. {
        deny all;
    }
}
